import ExtensionBlocker from "./components/ExtensionBlocker";

// App 컴포넌트: 전체 앱의 루트 컴포넌트
function App() {
  // ExtensionBlocker만 렌더링 (확장자 차단 기능)
  return (
    <ExtensionBlocker />
  );
}

export default App;
